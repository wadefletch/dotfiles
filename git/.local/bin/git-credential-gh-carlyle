#!/bin/sh
[ "$1" != "get" ] && exit 0

while IFS='=' read -r key value; do
    [ -z "$key" ] && break
done

echo "protocol=https"
echo "host=github.com"
echo "username=x-access-token"
echo "password=$(gh auth token --user wade-fletcher-cw_tcgemu)"
